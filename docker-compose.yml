services:
  enchilada:
    build:
      context: ../enchilada
    ports:
      - "8081:8081"
    volumes:
      - enchilada-db:/db
      - /Users/croeder/git/CCDA/tislab-clad/CCDA_OMOP_Private/CONCEPT.csv:/data/CONCEPT.csv:ro
      - /Users/croeder/claude_play/CCDA_OMOP_Private/CONCEPT_RELATIONSHIP.csv:/data/CONCEPT_RELATIONSHIP.csv:ro
      - /Users/croeder/git/matchbox_scripts/concept_extra.tsv:/data/concept_extra.tsv:ro
      - /Users/croeder/git/matchbox_scripts/concept_relationship_extra.tsv:/data/concept_relationship_extra.tsv:ro
      - /Users/croeder/git/matchbox_scripts/vocabulary_extra.tsv:/data/vocabulary_extra.tsv:ro
      - /Users/croeder/git/matchbox_scripts/certs:/certs:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request, ssl; ctx=ssl.create_default_context(); ctx.check_hostname=False; ctx.verify_mode=ssl.CERT_NONE; urllib.request.urlopen('https://localhost:8081/r4/metadata', context=ctx)"]
      interval: 10s
      timeout: 5s
      start_period: 120s
      retries: 12

  matchbox:
    image: croeder/matchbox:latest
    ports:
      - "8080:8080"
    volumes:
      - matchbox-db:/database
      - /Users/croeder/git/matchbox_docker/config/application.yaml:/config/application.yaml:ro
      - /Users/croeder/git/matchbox_scripts/certs/enchilada.jks:/certs/enchilada.jks:ro
    environment:
      - JAVA_TOOL_OPTIONS=-Djavax.net.ssl.trustStore=/certs/enchilada.jks -Djavax.net.ssl.trustStorePassword=changeit
    restart: unless-stopped
    depends_on:
      enchilada:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8080/matchboxv3/actuator/health"]
      interval: 10s
      timeout: 3s
      start_period: 300s
      retries: 6

  dqd:
    image: croeder/dqd:latest
    ports:
      - "3838:3838"
      - "8088:8088"
    volumes:
      - omop-db:/omop
    environment:
      - MATCHBOX_URL=http://matchbox:8080
      - OMOP_DB_PATH=/omop/omop.ddb
    depends_on:
      matchbox:
        condition: service_healthy

volumes:
  matchbox-db:
  omop-db:
  enchilada-db:
